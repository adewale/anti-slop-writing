# Card: Strip the new-register launch voice

## Before

```txt
We rebuilt the editor. No sign-ups, no downloads, no hassle. Zero config — it just works. Let's be honest: most editors are bloated. This one fits in your head. That's the whole point.
```

## After

```txt
We rebuilt the editor. It runs in the browser with no account: open the page, paste text, start typing. The interface is four commands, all listed on the first screen.
```

## Why it works

Every claim in the rewrite is checkable by the reader in under a minute (open the page, count the commands). The negation chain, the boilerplate (`zero config`, `it just works`, `fits in your head`), the candor opener, and the significance-compression closer all asserted qualities; the rewrite demonstrates them. `python3 evals/oracles/slop_lint.py` returns zero findings on the After text and six on the Before.
